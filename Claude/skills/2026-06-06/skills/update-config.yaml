# Claude Code Skill: update-config
slug: update-config
cmd: /update-config
trigger: "automated behavior requests"
desc: "Configure settings.json; handles hooks, permissions, env vars"
source: anthropics/claude-code@2.1.165
token_policy:
  - Omit repeated background context
  - Return only decision-critical code or instructions
  - Link to docs instead of inlining long content
