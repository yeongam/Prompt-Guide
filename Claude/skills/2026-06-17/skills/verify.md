# verify
cmd: /verify
trigger: user asks to verify a PR, confirm a fix works, test a change manually, check a feature works, or validate local changes before pushing
desc: Run app and observe behavior to verify code change; tests golden path and edge cases
note: Type checking and test suites verify code correctness, not feature correctness
