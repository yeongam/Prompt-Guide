---
name: cd
cmd: /cd <path>
trigger: user wants to move session to a different directory without losing prompt cache
added: v2.1.169
---
Moves active session working directory to `<path>` without breaking prompt cache continuity.
