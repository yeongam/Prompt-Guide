# loop

- Command: `/loop [interval] [/command]`
- Trigger: user wants recurring task (e.g. "check every 5m", "keep running X")
- Description: Run prompt or slash command on recurring interval (default 10m)
